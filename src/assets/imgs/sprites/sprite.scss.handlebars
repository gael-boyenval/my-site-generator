{{#extend "scss"}}
{{#content "sprites"}}
/*---------------------------------------------------------*\
    /!\ DO NOT EDIT : THIS FILE IS GENERATED BY GULP /!\
\*---------------------------------------------------------*/
{{#each sprites}}
${{strings.name}}: (
    x:{{px.x}},
    y:{{px.y}},
    offset_x:{{px.offset_x}},
    offset_y:{{px.offset_y}},
    width:{{px.width}},
    height:{{px.height}},
    total_width: {{px.total_width}},
    total_height: {{px.total_height}},
    escaped_image:'{{{escaped_image}}}',
    name:'{{name}}',
);
{{/each}}
{{/content}}
{{#content "spritesheet"}}
$sprite: ({{#each sprites}}${{strings.name}}, {{/each}});
{{/content}}
{{/extend}}
